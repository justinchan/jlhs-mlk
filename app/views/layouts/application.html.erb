<!DOCTYPE html>
<html>
<head>
  <title>MLK Registration 2014</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <%= link_to "MLK Registration 2014", root_path, :class=>"brand" %>
    <ul class="nav pull-right">
      <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Registration", root_path %></li>
            <li><%= link_to "Housekeeping", admin_path %></li>
          </ul>
        </li>
        <li class="active"><%= link_to "Logout", destroy_user_session_path, :method=>"delete" %></li>
      <% end %>
    </ul>
  </div>
</div>

<%= render partial: "layouts/messages", flash: flash %>
<%= yield %>

</body>
</html>
